{
  "name": "pokedex-vault-backend",
  "version": "1.0.0",
  "type": "module",
  "description": "Backend gratuito per Pokédex Vault — proxy verso TCGdex (nessuna API key richiesta)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2"
  },
  "engines": {
    "node": ">=18"
  }
}
